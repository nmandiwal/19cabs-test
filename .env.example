# Firebase service account JSON (single line)
cred={"type":"service_account",...}

# Dev RTDB URL
RTDB_URL=https://rapido-f614c-dev.asia-southeast1.firebasedatabase.app/

# Device serial (from `adb devices`)
DEVICE_SERIAL=emulator-5554

# Backend URLs
API_BASE_URL=http://localhost:3001
PRICING_BASE_URL=http://localhost:3002

# Metro dev server port (customer app)
DEV_SERVER_PORT=8082
